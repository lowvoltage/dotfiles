# Lowvoltage's dotfiles

----

Checkout in home folder using `git clone --recurse-submodules`

Then create symlinks:
* `ln -s ~/dotfiles/git/.gitconfig .`
* `ln -s ~/dotfiles/zsh/.zshrc .`
* `ln -s ~/dotfiles/vim/.vimrc .`

